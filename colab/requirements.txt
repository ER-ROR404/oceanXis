# --- Colab install set (colab/requirements.txt) ---
# Mirrors ml/pyproject.toml base + GPU torch for the Colab training host (ADR-009).
# Colab pre-installs a CUDA-enabled torch; `pip install -r` is idempotent and keeps it.
# Keep this list in sync with ml/pyproject.toml.

numpy>=1.26
xarray>=2024.1
netcdf4>=1.6
zarr>=2.17
pyyaml>=6.0
scipy>=1.12
scikit-learn>=1.4
pandas>=2.2
dask[array]>=2024.1
tqdm>=4.66
torch>=2.2